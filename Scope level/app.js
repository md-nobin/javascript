if (true){
    const username = "Unknown"
    if (username === "Unknown"){
        const address = "Dhaka"
        console.log(`User name: ${username}, User address: ${address}`)
    }

    // console.log(address)
}

// console.log(username)


addone(5)
function addone(num){
    return num + 1
}


// addTwo(5)  can't be execute
const addTwo = function(num){
    return num + 2
}
